global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1m
  receiver: 'slack-k8s-bot'

receivers:
- name: 'slack-k8s-bot'
  webhook_configs:
  - url: 'http://bot:8080/alertmanager'
    send_resolved: false