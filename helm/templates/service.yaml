apiVersion: v1
kind: Service
metadata:
  name: {{ include "slack-k8s-bot.fullname" . }}
  labels:
    {{- include "slack-k8s-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "slack-k8s-bot.selectorLabels" . | nindent 4 }}